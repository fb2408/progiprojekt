spot.bindPopup(`
<div class="container">
    <div class="col-3">
        <h5 class="mb-1">` + parking.parkingname + `</h5>
        <p class="mb-1">` + parking.description + `</p>
        <small>` + parking.hourlyprice + ` HRK/h</small>
        <div class="d-flex justify-content-between">
            <input type="datetime-local" id="calendar` + parking.id + "_" + parkingSpot.id.parkingspotnumber + `">
            <input type="checkbox" id="recurring` + parking.id + "_" + parkingSpot.id.parkingspotnumber + `">
        </div>
        <div class="d-flex justify-content-between">
            <button type="button" class="btn btn-secondary"
            onclick="sendPost(parking.id, parkingSpot.id.parkingspotnumber, false, document.getElementById('calendar\' + parking.id + '_' + parkingSpot.id.parkingspotnumber).value, document.getElementById('recurring' + parking.id + '_' + parkingSpot.id.parkingspotnumber))">Reserve</button>
            <button type="button" class="btn btn-primary"
            onclick="sendPost(parking.id, parkingSpot.id.parkingspotnumber, true, document.getElementById('calendar\' + parking.id + '_' + parkingSpot.id.parkingspotnumber).value, document.getElementById('recurring' + parking.id + '_' + parkingSpot.id.parkingspotnumber))">Reserve and Pay</button>
        </div>
    </div>
</div>
        `);